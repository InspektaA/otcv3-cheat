var serverIP = World.GetServerString();
var serverMap = World.GetMapName();

Cheat.PrintColor( [ 255, 0, 0, 255 ], "[onetap]Connected to " + serverIP );
Cheat.PrintColor( [ 255, 0, 0, 255 ], "[onetap]Map: " + serverMap );